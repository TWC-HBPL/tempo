apiVersion: v1
kind: Service
metadata:
  name: tempo-distributor
  namespace: observability
  labels:
    app: tempo-distributor
spec:
  selector:
    app: tempo-distributor
  ports:
    - name: otlp-http
      port: 4318
      targetPort: 4318
